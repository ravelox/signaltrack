{
  "name": "@signaltrack/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "@fastify/cookie": "^10.0.0",
    "@fastify/helmet": "^13.0.0",
    "zod": "^3.24.0",
    "@signaltrack/application": "workspace:*",
    "@signaltrack/infrastructure": "workspace:*",
    "@signaltrack/shared": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.10.0"
  }
}